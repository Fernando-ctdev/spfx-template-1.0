import * as React from 'react';
import { render, screen } from '@testing-library/react';
import useTest from '../src/core/hooks/useTest';

describe('useTest', () => {
  it('deve renderizar sem erros', () => {
    // Adicione seus testes aqui
  });

  // Adicione mais testes aqui
});
